# Тестовые схемы для замеров производительности

Сгенерировать из корня репозитория:

```bash
pnpm --dir demo gen:perf-schemes
```

Файлы `scheme-perf-*.json` появятся в этой папке. Подробная методика — `docs/эксперименты-производительность.md`.
